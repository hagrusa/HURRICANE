#ifndef CONSTANTS_H
#define CONSTANTS_H

#define BIG_G 1
#define COLLISIONS 0 // 0 if no inelastic collisions, 1 if collisions
#define DAMPENING 0  // 0 if no drag force, 1 if drag force
extern double SOFT_SQUARED;
extern double THETA;
#endif